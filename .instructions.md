---
applyTo: "src/**/*.py"
---

# Maintenance Notes

## Flet 0.85.3 Async Scroll
- `ft.Column.scroll_to()` is a **coroutine** — must be `await`ed.
- `scroll_to_bottom()` in `chat_ui.py` is `async def` and must be `await`ed at every call site.

## Settings Dialog
- Use `page.show_dialog(dialog)` instead of `page.dialog = dialog; dialog.open = True`.
